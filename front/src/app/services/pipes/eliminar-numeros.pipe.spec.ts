import { EliminarNumerosPipe } from './eliminar-numeros.pipe';

describe('EliminarNumerosPipe', () => {
  it('create an instance', () => {
    const pipe = new EliminarNumerosPipe();
    expect(pipe).toBeTruthy();
  });
});
